<Window x:Class="MyWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MyWpfApp.ViewModels"
        Title="نظام إدارة المنتجات"
        Height="600" Width="900"
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background"      Value="#2563EB"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="Padding"         Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"                 Value="#1E293B"/>
            <Setter Property="Foreground"                 Value="White"/>
            <Setter Property="Padding"                    Value="12,8"/>
            <Setter Property="FontWeight"                 Value="SemiBold"/>
            <Setter Property="FontSize"                   Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cell center style -->
        <Style x:Key="CellCenter" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment"   Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#1E293B">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="22" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="نظام إدارة المنتجات"
                               FontSize="18" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            VerticalAlignment="Center" HorizontalAlignment="Left">

                    <!-- بادج التحديث: يظهر فقط لو UpdateBadge مش فاضي -->
                    <Border Background="#EF4444" CornerRadius="12"
                            Padding="10,4" Margin="0,0,12,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding UpdateBadge}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <!-- ✅ Fix MC3072: استخدم DataTrigger بدل ZIndex -->
                                    <DataTrigger Binding="{Binding UpdateBadge, Converter={StaticResource NotEmptyConverter}}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding UpdateBadge}"
                                   Foreground="White" FontSize="12" FontWeight="Bold"
                                   Cursor="Hand">
                            <TextBlock.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding InstallUpdateCmd}"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </Border>

                    <Button Content="🔄 تحديث البيانات"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="16">

            <!-- ✅ Fix MC3072: استخدم Panel.ZIndex كـ Attached Property -->
            <Border Background="#80FFFFFF"
                    Panel.ZIndex="10"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="36" HorizontalAlignment="Center"/>
                    <TextBlock Text="جاري التحميل..." FontSize="16"
                               HorizontalAlignment="Center" Margin="0,8,0,0"
                               Foreground="#1E293B"/>
                </StackPanel>
            </Border>

            <DataGrid ItemsSource="{Binding Products}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      BorderThickness="1" BorderBrush="#E2E8F0"
                      Background="White"
                      RowBackground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#"
                                        Binding="{Binding Id}"
                                        Width="50"
                                        ElementStyle="{StaticResource CellCenter}"/>

                    <DataGridTextColumn Header="اسم المنتج"
                                        Binding="{Binding Name}"
                                        Width="*"/>

                    <DataGridTextColumn Header="الفئة"
                                        Binding="{Binding Category}"
                                        Width="120"
                                        ElementStyle="{StaticResource CellCenter}"/>

                    <DataGridTextColumn Header="السعر"
                                        Binding="{Binding PriceFormatted}"
                                        Width="130"
                                        ElementStyle="{StaticResource CellCenter}"/>

                    <DataGridTextColumn Header="المخزون"
                                        Binding="{Binding Stock}"
                                        Width="80"
                                        ElementStyle="{StaticResource CellCenter}"/>

                    <DataGridTextColumn Header="الحالة"
                                        Binding="{Binding StockStatus}"
                                        Width="110"
                                        ElementStyle="{StaticResource CellCenter}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- STATUS BAR -->
        <Border Grid.Row="2" Background="#1E293B">
            <Grid Margin="16,0">
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="#94A3B8" FontSize="12"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding AppVersion}"
                           Foreground="#64748B" FontSize="12"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
